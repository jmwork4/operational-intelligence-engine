[project]
name = "oie-integrations"
version = "0.1.0"
description = "Integrations framework for the Operational Intelligence Engine"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "jinja2>=3.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
